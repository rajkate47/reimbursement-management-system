{
  "name": "reimbursement-management-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack reimbursement management platform with SQLite persistence and a browser dashboard.",
  "type": "commonjs",
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server.js",
    "dev": "node --watch --no-warnings=ExperimentalWarning server.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
